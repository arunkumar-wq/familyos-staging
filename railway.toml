[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && cd client && npm install --include=dev && npm run build && cd .. && node database/seed.js"

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "on_failure"
